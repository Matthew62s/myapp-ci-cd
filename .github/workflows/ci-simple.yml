name: CI Simple

on:
  push:
    branches: [ main ]

jobs:
  basic-checks:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Check files exist
      run: |
        echo "📁 Checking project structure..."
        ls -la
        echo "🔍 Checking key files..."
        [ -f "Dockerfile" ] && echo "✅ Dockerfile exists" || echo "❌ Dockerfile missing"
        [ -f "requirements.txt" ] && echo "✅ requirements.txt exists" || echo "❌ requirements.txt missing"
        [ -f "src/app.py" ] && echo "✅ src/app.py exists" || echo "❌ src/app.py missing"
        [ -d "k8s/manifests" ] && echo "✅ k8s/manifests exists" || echo "❌ k8s/manifests missing"
    
    - name: Simple validation
      run: |
        echo "✅ CI validation passed!"
        echo "All necessary files are present"
